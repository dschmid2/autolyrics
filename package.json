{
  "name": "autolyrics",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build:version": "node scripts/generate-version.js",
    "build:css": "tailwindcss -i ./src/styles.css -o ./public/styles.css --minify",
    "build:js": "esbuild src/index.jsx --bundle --minify --jsx=automatic --outfile=public/bundle.js",
    "build": "npm run build:version && npm run build:css && npm run build:js",
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "lucide-react": "^0.383.0",
    "multer": "^2.2.0",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "tailwindcss": "^3.4.10"
  }
}
